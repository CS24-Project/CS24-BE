server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://studentvote2.cz4muuwq2azg.ap-northeast-2.rds.amazonaws.com:3306/studentvote?serverTimezone=Asia/Seoul
    username: admin
    password: studentvote!
  sql:
    init:
      platform: mysql

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

kakao:
  client-id: ${KAKAO_CLIENT_ID}
  client-secret: ${KAKAO_CLIENT_SECRET}
  redirect-uri: ${KAKAO_REDIRECT_URI}